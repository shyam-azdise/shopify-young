{% if article.tags.size > 0 %}
  <div class="blog-category-2">
    {% for tag in article.tags %}
      <a href="{{ blog.url }}/tagged/{{ tag | handle }}">{{ tag | join: ', ' }}</a>
    {% endfor %}
  </div>
{% endif %}
